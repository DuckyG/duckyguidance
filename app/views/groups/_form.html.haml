= form_for(@group) do |f|
  - if @group.errors.any?
    #errorExplanation
      %h2= "#{pluralize(@category.errors.count, 'error')} prohibited this user from being saved:"
      %ul
      - @group.errors.full_messages.each do |msg|
        %li= msg
  
  .field
    = f.label :name
    %br
    = f.text_field :name
  .field
    = f.label :description
    %br
    = f.text_area :description

  .actions
    = f.submit 'Save'
