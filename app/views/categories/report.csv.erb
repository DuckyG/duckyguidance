Note Date,Group,Student Last Name,Student First Name,Summary,Author,Tags,Note
<%-@notes.sort{|x,y| x.created <=> y.created_at}.each do |note|%>
<%- if note.groups.count == 1 ||note.students.count == 1 -%>
<%=raw(note.to_csv)-%>
<%-end-%>
<%- if note.students.count > 1
     note.students.each do |student|
-%>
<%=raw(note.to_csv(student))-%>
<%-
    end
  end-%>
<%- end -%><%#Do not add new lines, otherwise, there will be empty lines in the CSV%>